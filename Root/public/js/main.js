const db = require('../../db/connection');

document.addEventListener('DOMContentLoaded', () => {
    console.log("Homepage loaded. Ready for further interactions!");
  });
  document.addEventListener('DOMContentLoaded', () => {
    console.log("Homepage loaded with vibrant colors and workout section!");
  });
